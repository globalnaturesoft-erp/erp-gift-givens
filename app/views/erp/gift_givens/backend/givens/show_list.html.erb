<div class="text-right mb-10">
  <a target="_blank"
    href="<%= erp_gift_givens.xlsx_backend_givens_path(params.to_unsafe_hash.merge({format: 'xlsx'})) %>"
    target="_blank"
    class="btn btn-primary"
  >Xuất excel</a>
</div>
<iframe class="pdf" src="<%= erp_gift_givens.pdf_backend_givens_path(params.to_unsafe_hash.merge({format: 'pdf'})) %>" />